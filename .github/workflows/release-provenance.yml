# SPDX-FileCopyrightText: 2026 Xquik contributors
#
# SPDX-License-Identifier: Apache-2.0

name: Release Provenance

permissions: {}

concurrency:
  group: release-provenance
  cancel-in-progress: false

on:
  push:
    branches:
      - main
    paths:
      - ".release-please-manifest.json"
  workflow_dispatch:
    inputs:
      release_ref:
        description: Exact release tag to publish
        required: true
        type: string
      release_target_sha:
        description: Commit validated by the write-scoped prepare job
        required: true
        type: string

jobs:
  prepare:
    name: Prepare Draft Release
    if: github.event_name == 'push'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: write # Dispatch the tag-bound publisher.
      contents: write # Create the exact release tag.
    steps:
      - name: Check out release commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Prepare immutable draft
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          version="$(jq -er '."."' .release-please-manifest.json)"
          [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z][0-9A-Za-z.-]*)?$ ]]
          release_tag="v${version}"
          release_json=""

          for _ in {1..30}; do
            if release_json="$(
              gh api --paginate --slurp \
                "repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
                2>/dev/null |
                jq -ce --arg tag "$release_tag" '
                  [.[][] | select(.tag_name == $tag)]
                  | if length == 1 then
                      {
                        isDraft: .[0].draft,
                        tagName: .[0].tag_name,
                        targetCommitish: .[0].target_commitish
                      }
                    else
                      empty
                    end
                '
            )"; then
              break
            fi
            sleep 10
          done

          test -n "$release_json"
          test "$(jq -r '.isDraft' <<<"$release_json")" = "true"
          test "$(jq -r '.tagName' <<<"$release_json")" = "$release_tag"
          test "$(jq -r '.targetCommitish' <<<"$release_json")" = "$GITHUB_SHA"

          if remote_sha="$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/tags/${release_tag}" --jq '.object.sha' 2>/dev/null)"; then
            test "$remote_sha" = "$GITHUB_SHA"
          else
            gh api --method POST "repos/${GITHUB_REPOSITORY}/git/refs" \
              -f ref="refs/tags/${release_tag}" \
              -f sha="$GITHUB_SHA" >/dev/null
          fi

          gh workflow run release-provenance.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref "$release_tag" \
            -f release_ref="$release_tag" \
            -f release_target_sha="$GITHUB_SHA"

  attest:
    name: Build & Attest Composer Archive
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    outputs:
      archive_name: ${{ steps.release.outputs.archive_name }}
      bundle_name: ${{ steps.release.outputs.bundle_name }}
      release_tag: ${{ steps.release.outputs.release_tag }}
    permissions:
      attestations: write # Required to publish SLSA provenance.
      contents: read
      id-token: write # Required for GitHub keyless signing.
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.release_ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Validate release tag
        id: release
        env:
          RELEASE_TAG: ${{ inputs.release_ref }}
          RELEASE_TARGET_SHA: ${{ inputs.release_target_sha }}
        run: |
          set -euo pipefail

          if [[ "$GITHUB_REF_TYPE" != "tag" || "$RELEASE_TAG" != "$GITHUB_REF_NAME" ]]; then
            echo "Run this workflow from the matching release tag." >&2
            exit 1
          fi
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.+-]+)?$ ]]; then
            echo "Release tag is invalid. Use a v-prefixed semantic version." >&2
            exit 1
          fi
          version="$(jq -er '."."' .release-please-manifest.json)"
          test "$RELEASE_TAG" = "v${version}"
          test "$RELEASE_TARGET_SHA" = "$GITHUB_SHA"
          test "$(git rev-list -n 1 "$RELEASE_TAG")" = "$GITHUB_SHA"
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main
          {
            echo "archive_name=x-twitter-scraper-php-$RELEASE_TAG.zip"
            echo "bundle_name=x-twitter-scraper-php-$RELEASE_TAG.sigstore.json"
            echo "release_tag=$RELEASE_TAG"
          } >> "$GITHUB_OUTPUT"

      - name: Set up PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.5"
          coverage: xdebug
          extensions: curl, mbstring, xml, zip
          tools: composer:2.10.2

      - name: Install locked dependencies
        run: ./scripts/bootstrap

      - name: Run release quality gates
        run: composer check

      - name: Check repository licensing
        uses: fsfe/reuse-action@676e2d560c9a403aa252096d99fcab3e1132b0f5 # v6.0.0

      - name: Build reproducible Composer archive
        env:
          ARCHIVE_NAME: ${{ steps.release.outputs.archive_name }}
        run: ./scripts/build-archive "$RUNNER_TEMP/$ARCHIVE_NAME"

      - name: Attest Composer archive
        id: provenance
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: ${{ runner.temp }}/${{ steps.release.outputs.archive_name }}

      - name: Stage attestation bundle
        env:
          ATTESTATION_BUNDLE: ${{ steps.provenance.outputs.bundle-path }}
          BUNDLE_NAME: ${{ steps.release.outputs.bundle_name }}
        run: cp -- "$ATTESTATION_BUNDLE" "$RUNNER_TEMP/$BUNDLE_NAME"

      - name: Preserve attested release files
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: composer-release-${{ steps.release.outputs.release_tag }}
          path: |
            ${{ runner.temp }}/${{ steps.release.outputs.archive_name }}
            ${{ runner.temp }}/${{ steps.release.outputs.bundle_name }}
          if-no-files-found: error
          retention-days: 1

  attach:
    name: Attach Attested Release Files
    needs: attest
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      actions: read # Required to download this run's release artifact.
      contents: write # Required to attach files to the matching release.
    steps:
      - name: Download attested release files
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: composer-release-${{ needs.attest.outputs.release_tag }}
          path: release-assets

      - name: Attach files to GitHub release
        env:
          ARCHIVE_NAME: ${{ needs.attest.outputs.archive_name }}
          BUNDLE_NAME: ${{ needs.attest.outputs.bundle_name }}
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.attest.outputs.release_tag }}
        run: |
          gh release upload "$RELEASE_TAG" \
            "release-assets/$ARCHIVE_NAME" \
            "release-assets/$BUNDLE_NAME" \
            --repo "$GITHUB_REPOSITORY" \
            --clobber
          gh release edit "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --draft=false \
            --latest \
            --verify-tag
